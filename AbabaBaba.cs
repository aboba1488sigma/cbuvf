using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbabaBaba : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       //вывод приветстыие миру
        
    }

    // Update is called once per frame
    void Update()
    {
        print("crickets");
    }
}
